{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "Backend in express for the phonebook app",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"I didn't implement this thing yet\" && exit 0",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../frontend-phonebook && npm run build && cp -r dist ../backend-phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m buildui && git push",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  }
}
